test: mvp
<!-- 完成伪代码撰写 -->
<!-- mvp: non db, non auth
前端人齐，告诉后端
后端调用startgame
后端获取状态，给到前端
前端渲染
前端调用 emb or imp
后端检查和 game 状态的 curr 一致，
    后端调用；
    后端获取状态，给到前端
    前端渲染
不一致
    返回错误
    前端处理
前端调用 play
后端检查 game 状态的 curr 一致，
    调用；
    后端获取状态，尤其是 move type 给到前端；
    前端根据 move type 渲染
    前端获取用户选择，将用户选择给到后端
    后端 parse 后调用 game 对应方法，注意传递 params
    后端获取状态，返回前端
    前端渲染
不一致
    返回错误
    前端处理

登录暂时都不写，一个人可以用好多号


传到github，托管
在这里开发，再传到 github 上，再由云托管拉下来更新发布
注意不用云开发。云开发的那个cloud记得删除！ -->